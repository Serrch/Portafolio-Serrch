import { Archivo } from "next/font/google";
import { Outfit } from "next/font/google";

export const archivo = Archivo({ weight: "400", subsets: ["latin"] });
export const outfit = Outfit({ weight: "400", subsets: ["latin"] });
